# Intentionally empty.
# The original file used the pre-Odoo-18 `_pos_ui_models_to_load` /
# `_loader_params_*` API which was removed in the new POS data loader
# (Odoo 18+). Keeping this file empty so accidental re-imports do not
# crash module installation. Do not import this module from __init__.py.
